// AUTOGENERATED — DO NOT EDIT
// Source:     octomil-contracts/dist/openapi.yaml
// Generator:  openapi-generator-cli==7.10.0 (jvm-okhttp4)
// Regenerate: octomil-contracts/tools/sdkgen/generate-kotlin.sh

/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.octomil.generated.transport.models

import ai.octomil.generated.transport.models.CandidateGateSchema
import ai.octomil.generated.transport.models.RuntimePlanResponseSchemaRuntimeArtifactPlan

import kotlinx.serialization.Serializable
import kotlinx.serialization.SerialName
import kotlinx.serialization.Contextual

/**
 * A single candidate in a runtime plan, representing either a local engine or cloud inference.
 *
 * @param locality Where this candidate would run inference.
 * @param priority Candidate priority. Lower is higher priority.
 * @param confidence Server confidence score for this candidate (0.0-1.0).
 * @param reason Human-readable reason for this recommendation.
 * @param engine Canonical engine identifier (e.g. 'mlx-lm', 'llama.cpp', 'cloud').
 * @param engineVersionConstraint Semver constraint on engine version (e.g. '>=0.18.0').
 * @param artifact Recommended model artifact to download or use.
 * @param benchmarkRequired Whether a local benchmark is required before the SDK should use this candidate.
 * @param gates Gate requirements the SDK must evaluate per-request before selecting this candidate. Gates are checked in the attempt loop; failure of a required gate causes the attempt to fail and fallback to the next candidate.
 * @param deliveryMode How the SDK should reach this candidate. 'hosted_gateway' = call api.octomil.com /v1 routes; 'sdk_runtime' = run on-device with a prepared local runtime + artifact; 'external_endpoint' = caller-supplied OpenAI-compatible URL.
 * @param prepareRequired Whether the SDK must prepare (install runtime + stage artifacts) before this candidate can serve a request. Always false for hosted_gateway. Typically true for sdk_runtime when the artifact is not yet staged.
 * @param preparePolicy When the SDK is allowed to prepare this candidate. 'lazy' = at first .create() request; 'explicit_only' = only via client.prepare()/initialize(warm=...); 'disabled' = never prepare (e.g. hosted_gateway).
 */
@Serializable

data class RuntimePlanResponseSchemaRuntimeCandidatePlan (

    /* Where this candidate would run inference. */
    @SerialName(value = "locality")
    val locality: RuntimePlanResponseSchemaRuntimeCandidatePlan.Locality,

    /* Candidate priority. Lower is higher priority. */
    @SerialName(value = "priority")
    val priority: kotlin.Int,

    /* Server confidence score for this candidate (0.0-1.0). */
    @Contextual @SerialName(value = "confidence")
    val confidence: java.math.BigDecimal,

    /* Human-readable reason for this recommendation. */
    @SerialName(value = "reason")
    val reason: kotlin.String,

    /* Canonical engine identifier (e.g. 'mlx-lm', 'llama.cpp', 'cloud'). */
    @SerialName(value = "engine")
    val engine: kotlin.String? = null,

    /* Semver constraint on engine version (e.g. '>=0.18.0'). */
    @SerialName(value = "engine_version_constraint")
    val engineVersionConstraint: kotlin.String? = null,

    /* Recommended model artifact to download or use. */
    @Contextual @SerialName(value = "artifact")
    val artifact: RuntimePlanResponseSchemaRuntimeArtifactPlan? = null,

    /* Whether a local benchmark is required before the SDK should use this candidate. */
    @SerialName(value = "benchmark_required")
    val benchmarkRequired: kotlin.Boolean? = false,

    /* Gate requirements the SDK must evaluate per-request before selecting this candidate. Gates are checked in the attempt loop; failure of a required gate causes the attempt to fail and fallback to the next candidate. */
    @SerialName(value = "gates")
    val gates: kotlin.collections.List<@Contextual CandidateGateSchema>? = null,

    /* How the SDK should reach this candidate. 'hosted_gateway' = call api.octomil.com /v1 routes; 'sdk_runtime' = run on-device with a prepared local runtime + artifact; 'external_endpoint' = caller-supplied OpenAI-compatible URL. */
    @SerialName(value = "delivery_mode")
    val deliveryMode: RuntimePlanResponseSchemaRuntimeCandidatePlan.DeliveryMode? = null,

    /* Whether the SDK must prepare (install runtime + stage artifacts) before this candidate can serve a request. Always false for hosted_gateway. Typically true for sdk_runtime when the artifact is not yet staged. */
    @SerialName(value = "prepare_required")
    val prepareRequired: kotlin.Boolean? = false,

    /* When the SDK is allowed to prepare this candidate. 'lazy' = at first .create() request; 'explicit_only' = only via client.prepare()/initialize(warm=...); 'disabled' = never prepare (e.g. hosted_gateway). */
    @SerialName(value = "prepare_policy")
    val preparePolicy: RuntimePlanResponseSchemaRuntimeCandidatePlan.PreparePolicy? = PreparePolicy.LAZY

) : kotlin.collections.HashMap<String, kotlinx.serialization.json.JsonElement>() {

    /**
     * Where this candidate would run inference.
     *
     * Values: LOCAL,CLOUD
     */
    @Serializable
    enum class Locality(val value: kotlin.String) {
        @SerialName(value = "local") LOCAL("local"),
        @SerialName(value = "cloud") CLOUD("cloud");
    }
    /**
     * How the SDK should reach this candidate. 'hosted_gateway' = call api.octomil.com /v1 routes; 'sdk_runtime' = run on-device with a prepared local runtime + artifact; 'external_endpoint' = caller-supplied OpenAI-compatible URL.
     *
     * Values: HOSTED_GATEWAY,SDK_RUNTIME,EXTERNAL_ENDPOINT
     */
    @Serializable
    enum class DeliveryMode(val value: kotlin.String) {
        @SerialName(value = "hosted_gateway") HOSTED_GATEWAY("hosted_gateway"),
        @SerialName(value = "sdk_runtime") SDK_RUNTIME("sdk_runtime"),
        @SerialName(value = "external_endpoint") EXTERNAL_ENDPOINT("external_endpoint");
    }
    /**
     * When the SDK is allowed to prepare this candidate. 'lazy' = at first .create() request; 'explicit_only' = only via client.prepare()/initialize(warm=...); 'disabled' = never prepare (e.g. hosted_gateway).
     *
     * Values: LAZY,EXPLICIT_ONLY,DISABLED
     */
    @Serializable
    enum class PreparePolicy(val value: kotlin.String) {
        @SerialName(value = "lazy") LAZY("lazy"),
        @SerialName(value = "explicit_only") EXPLICIT_ONLY("explicit_only"),
        @SerialName(value = "disabled") DISABLED("disabled");
    }

}

